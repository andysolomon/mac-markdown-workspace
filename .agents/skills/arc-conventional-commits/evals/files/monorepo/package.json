{
  "name": "my-monorepo-root",
  "version": "0.0.0",
  "private": true,
  "description": "Root package of an npm workspaces monorepo.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"no tests\" && exit 0"
  },
  "devDependencies": {}
}
