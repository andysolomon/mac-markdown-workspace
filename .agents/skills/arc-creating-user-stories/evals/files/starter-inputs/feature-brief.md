# Feature brief

The app is a lightweight personal finance dashboard.

Current gaps:

- Users manually categorize transactions after import.
- There is no monthly budget alert when a category is close to its limit.
- The dashboard has no empty state for first-time users.

Constraints:

- Use GitHub Issues only after the user approves the draft backlog.
- Keep stories small enough for independent implementation.
