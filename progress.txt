Mac Markdown Workspace — Design Overhaul Progress
Generated: 2026-07-05

Legend: [x] done  [~] in progress  [ ] not started
Scope: rebuild the single-doc editor into a Bear-style three-pane notes
workspace on the "Mac Markdown" design system (4 light themes + derived dark),
across Electron / web / iOS. Plan: IMPLEMENTATION_PLAN.md.

[x] 0.0 - Phase 0 - Foundation (v1, shipped)
    [x] 0.1 - React 19 app on window.appApi abstraction (Electron/web/iOS shims)
    [x] 0.2 - Single-doc store, CodeMirror source, Milkdown WYSIWYG, react-markdown preview
    [x] 0.3 - File open/save/saveAs + TXT/PDF/DOCX export per platform
    [x] 0.4 - light/dark/system themes (color-only tokens)
    [x] 0.5 - Web entry wiring fixed + regression test (src/tests/webEntry.test.ts)

[x] 1.0 - Phase 1 - Token foundation + dark derivation
    [x] 1.1 - Copy kit tokens into src/styles/tokens/{colors,typography,spacing,markdown}.css + tokens entry (index.css)
    [x] 1.2 - Derive dark variants for all four palettes (colors-dark.css, data-mode="dark") — approved by author
    [x] 1.3 - Rework themeStore to {palette, mode} + stamp data-theme + data-mode on <html>; palette/mode toolbar controls; persist
    [x] 1.4 - Interactive preview harness (all 8 palette x mode combos) delivered for review
    [x] 1.5 - Acceptance: verified live in-app (Forest/Light + Teal/Dark), persists across reload; compat.css bridges legacy chrome; no raw hex outside token files

[x] 2.0 - Phase 2 - Typography + base
    [x] 2.1 - Brand type applied (--mm-font-sans / --mm-font-mono) + antialiasing; reading panes use 20px --mm-text-base
    [x] 2.2 - Reconcile src/index.css spacing/radii to --mm-space-*/--mm-radius-* tokens
    [x] 2.3 - Acceptance: Avenir Next renders (Nunito Sans fallback), preview at 20px; verified light + dark; web + iOS builds pass

[~] 3.0 - Phase 3 - Notes library data model + persistence (backbone)
    [x] 3.1 - Note model + derivation (title/preview/#tags) in notesModel.ts; 14 unit tests
    [x] 3.2 - Extend AppApi: listNotes/readNote/createNote/writeNote/deleteNote (RawNote)
    [~] 3.3 - Electron impl (Documents/Mac Markdown/*.md via fs) — code complete; runtime check pending desktop launch
    [x] 3.4 - Web impl (IndexedDB) — VERIFIED persists across reload + CRUD round-trip. (File System Access "real folder" for Chrome/Edge deferred)
    [~] 3.5 - iOS impl (Capacitor Filesystem, Documents/notes) — code complete; runtime check pending simulator
    [x] 3.6 - notesStore (Zustand) + welcome-note seeding; 5 unit tests. (editor<->note autosave wiring lands in Phase 5)
    [~] 3.7 - Acceptance: web verified; Electron/iOS runtime pending; typecheck + 38 tests + web/iOS builds pass

[x] 4.0 - Phase 4 - Tag + search engine
    [x] 4.1 - buildTagIndex: counted, alphabetical tag index across the library (case-insensitive merge)
    [x] 4.2 - notesStore selectedTag + searchQuery state/setters; token search over title+body
    [x] 4.3 - filterNotes + selectFilteredNotes / selectTagIndex derived selectors
    [x] 4.4 - Acceptance: verified by unit tests (46 total); typecheck, lint, web build pass

[x] 5.0 - Phase 5 - Three-pane shell UI
    [x] 5.1 - Port kit components (WindowDots, Tag, DocListItem, ThemeSwatch, MarkdownLine) — typed TSX in src/components/shell/ (delegated to Composer worker; verified + accepted)
    [x] 5.2 - Sidebar (dots, All pill, HASHTAGS, tag list)
    [x] 5.3 - DocList (search + rows) wired to notesStore via NotesShell container
    [x] 5.4 - EditorChrome (list icon toggles list, Aa stub for Phase 7, + creates note); NotesShell: selection->buffer sync + debounced autosave with flush-on-switch; Open/drop/Cmd+N now import into the library (prevents autosave overwriting the active note)
    [x] 5.5 - Responsive collapse: sidebar hidden on mobile; doc list toggles as a full-width sheet
    [x] 5.6 - Acceptance: verified live (create/edit/autosave/tags/filter/search/reload-persist); Teal/Light + Crimson/Dark screenshots match mockup; 46 tests, typecheck, lint, web + iOS builds pass
    Note: legacy toolbar remains temporarily inside the editor column (actions migrate in Phases 6-7); editor content colors are Phase 6; no delete-note UI yet

[x] 6.0 - Phase 6 - Structure-colored Markdown editor
    [x] 6.1 - CodeMirror HighlightStyle (markdownEditorTheme.ts): all syntax marks (processingInstruction) in --md-marker, headings/links/code/quotes per --md-* roles, prose neutral; oneDark removed; gutters off (distraction-free); sans 20px editor via --mm-font-editor/--mm-editor-size vars
    [x] 6.2 - Preview + WYSIWYG mapped to --md-* roles (accent H1/::marker bullets/checkbox accent-color, weight-carried bold, dimmed strike, quote bar, table header rule, themed code)
    [>] 6.3 - Aa font popover -> moved to Phase 7 (duplicate of 7.2; the --mm-font-editor/--mm-editor-size hooks are in place)
    [x] 6.4 - Acceptance: verified live in split view, Teal/Light + Gold/Dark — marks accent, prose neutral, edit + read consistent. Fixed CodeMirror duplicate-instance bug via vite resolve.dedupe (all 3 configs) + package.json overrides. 46 tests, typecheck, lint, web + iOS builds pass

[x] 7.0 - Phase 7 - Theme + font switcher UX
    [x] 7.1 - ThemeSwatch two-tone palette picker + Light/Dark/System pills in the Aa popover; palette/mode buttons removed from the legacy toolbar
    [x] 7.2 - Aa popover (FontPopover): 9 curated faces each rendered in its own font, A-/A+ size stepper (16-28px, clamped); --mm-shadow-pop card
    [x] 7.3 - themeStore extended with {font, size}; App applies --mm-font-editor/--mm-editor-size; all four settings persist via AppApi (electron-store / localStorage per platform)
    [x] 7.4 - Acceptance: verified live on web — Forest/Dark/Georgia/23px round-trips across reload; rapid-click stepper stale-closure bug found+fixed; Electron/iOS persistence via same AppApi settings path (runtime pass in Phase 8). 46 tests, typecheck, lint, web + iOS builds pass

[x] 8.0 - Phase 8 - Feedback round: export overhaul, save semantics, pane-collapse fix (author feedback 2026-07-05)
    [x] 8.1 - Pane collapse fixed (issue #1): matchMedia listener; verified live — narrow collapses, widening restores both panes
    [x] 8.2 - exportHtml on AppApi + all 3 shims (issue #2); standalone themed doc verified: derived title, inlined tokens, self-contained
    [x] 8.3 - Web exportPdf prints a standalone note-only document via hidden frame (issue #3); Electron PDF now uses the same themed doc; print-preview eyeball deferred to Phase 11 real-browser pass
    [x] 8.4 - Export menu: HTML + PDF + TXT + DOCX (desktop + hamburger); HTML path verified end-to-end to the shim boundary
    [x] 8.5 - Save = flush to library, no dialog (issue #4, verified: no picker, status Saved); iOS iCloud storage + entitlement steps documented in docs/ios-icloud.md (issue #5)

[x] 9.0 - Phase 9 - Settings screen
    [x] 9.1 - SettingsPanel (issue #6): "..." entry in topbar, popover design language, outside-click close
    [x] 9.2 - Toolbar show/hide toggle (issue #7): verified live — hides instantly, persists across reload, restores; shortcuts unaffected
    [x] 9.3 - iOS save-location (issue #8, #22): code merged — canonical "documents"/"private" -> Directory.Documents vs Directory.LibraryNoCloud (Directory.Data no longer used; it aliased Documents), legacy icloud/device normalized + device data migrated, copy->verify->activate->cleanup migration with marker-based retry, serialized notes I/O, transactional Settings pills ("Documents & Backup" / "On device", flush + reload, error keeps old selection); 33 new tests (src/tests/iosNotesStorage.test.ts, SettingsPanel.test.tsx); docs corrected; PR #22 squash-merged on main. OUTSTANDING: simulator/device evidence of physically distinct roots + relaunch persistence (docs/archive/W-000008-progress.txt 5.2–5.4) — close on issue #8 when recorded.
    [x] 9.4 - Acceptance (web): panel + toggle verified live; Storage section correctly absent on web; 46 tests, typecheck, lint, web + iOS builds pass

[x] 10.0 - Phase 10 - Mobile editing kit: bottom bar + markdown keyboard accessory
    [x] 10.1 - BottomBar (issue #9): share menu (HTML/PDF/TXT/DOCX via shared exportActions), Aa -> font popover, + -> new note; CSS-primitive share glyph; mobile-only
    [x] 10.2 - MarkdownAccessoryBar (issue #10): #, B, I, list, task, quote, code, link, indent + sticky Done; inserts at the CodeMirror cursor via editorBridge (wrap/toggle-prefix/indent/link); focus preserved through taps (mousedown preventDefault)
    [x] 10.3 - visualViewport-aware bottom offset (tracks the iOS keyboard); popovers bottom-anchored on mobile
    [x] 10.4 - Acceptance: verified on 390px web viewport — bars swap on editor focus/Done, heading/task/bold insert + toggle correctly with focus kept, Done always visible, neither bar renders on desktop; 46 tests, typecheck, lint, web + iOS builds pass. iOS device pass in Phase 11

[x] 10.6 - Feedback round 2 (author iPad testing, 2026-07-05)
    [x] 10.6.1 - Default view mode is Source (issue #11); verified live
    [x] 10.6.2 - iOS Safari export (issue #12): DOM-attached download anchor + delayed URL revoke; share-sheet delivery (navigator.share w/ files) on touch; PDF pre-opens a tab synchronously in the click stack (static exportActions import preserves transient activation) and prints there — touch-WebKit branch verified in Playwright WebKit (popup + themed doc); REAL-DEVICE CONFIRMATION PENDING (issue open)
    [x] 10.6.3 - Maximize editor (issue #13): list icon now toggles BOTH panels; verified live — full-width editor and restore; mobile overlay preserved

[x] 11.0 - Phase 11 - Cross-platform verification, migration, cleanup
    [x] 11.1 - Runtime passes: web (continuous); Electron via CDP — shell/theme/settings verified, notes are real .md files in ~/Documents/Mac Markdown (created/read/cleaned). Found+fixed StrictMode double-seed race in loadLibrary (+regression test). iOS: builds green; device pass checklist handed to author (issues #8, #12 remain open for it)
    [x] 11.2 - Dead v1 chrome retired: "Unsaved document" -> active note title; Open renamed Import (library semantics)
    [x] 11.3 - README.md + CLAUDE.md rewritten for the notes-workspace architecture (incl. CM6 dedupe, iOS activation, StrictMode seeding, :root role-resolution gotchas)
    [x] 11.4 - Tests 46 -> 56: themeStore, settings/app defaults, buildStandaloneHtml, loadLibrary concurrency; matchMedia setup stub; webEntry guard intact
    [x] 11.5 - Real Safari (26.4) vs production: shell renders, default-Source live, notes CRUD round-trip in IndexedDB
    [x] 11.6 - Acceptance: typecheck, lint, 56 unit tests, web + iOS builds, and all 5 Playwright e2e specs (updated to the new shell, run against the packaged app) pass

[x] 12.0 - Post-overhaul polish (2026-07-06)
    [x] 12.1 - Portfolio case study rewritten for the notes-workspace redesign (new hero + dark shots); live on andrewsolomon.dev
    [x] 12.2 - Delete-note affordance on doc rows (hover x, faint on touch, confirm) — verified live
    [x] 12.3 - Export feedback toast ("Exported document.html" / "Opened print view" / "Export failed") — verified live; immediately caught 12.4
    [x] 12.4 - Build collision fixed: iOS build clobbered dist-web (both configs shared outDir), so local previews after ios:build served the Capacitor bundle. iOS now outputs dist-ios (capacitor webDir updated); verified bundles distinct

[x] 13.0 - Device feedback round (author iPhone testing, 2026-07-06)
    [x] 13.1 - Accessory bar: Done removed (iOS provides dismissal); # and other line prefixes leave the caret AFTER the prefix (verified: Col 1 -> Col 3)
    [x] 13.2 - Popover trigger toggle race fixed (Aa + settings close on second tap; outside-close preserved)
    [x] 13.3 - Mobile chrome dedupe: topbar Aa/+ hidden at <=640px (bottom bar owns them)
    [~] 13.4 - Keyboard viewport (issue #14/W-000015): --mm-kb-inset published from visualViewport (+window scroll), editor scroller pads by it, caret re-revealed on inset changes and inserts — mechanics verified on web; real-keyboard geometry confirmation on device pending

[x] 14.0 - Bear navigation + palette expansion (author feedback round 3, 2026-07-06)
    [x] 14.1 - Mobile page stack (issue #16): sidebar > list > editor as full pages; chevrons on All/tag rows; list page header (back, title, +); bottom bar gains back; hamburger menu removed; toolbar hidden on mobile. Verified full flow at 390px; desktop split layout unchanged
    [x] 14.2 - Four new palettes (issue #17): Blue, Olive, Graphite, Red in colors-extra.css (light + dark each); 8 swatches in Aa popover; App palette validation now list-driven. Verified all four resolve tokens in dark mode

[x] 15.0 - Simulator feedback round (issue #18 / W-000018, 2026-07-06)
    [x] 15.1 - Editor page top quieted: list icon hidden on mobile (read as a hamburger; bottom-bar back owns navigation); settings "..." stays
    [x] 15.2 - Swipe-to-delete on doc rows (touch translate + revealed destructive action, confirm-guarded); hover x remains for desktop; verified via synthetic touch (reveal -84px, delete works)
    [x] 15.3 - Bottom bar normalized: four equal flex cells, uniform height/size/weight/accent color
    [x] 15.4 - Toolbar toggle hidden in mobile settings (desktop-only preference)
    [~] 15.5 - Native keyboard: @capacitor/keyboard added, resize "native" (webview shrinks -> accessory bar stays above keyboard), Keyboard.setStyle follows light/dark mode — REQUIRES bun run ios:sync (new pod); device confirmation pending with #14

[x] 16.0 - Passwordless vault sync (docs/passwordless-vault-sync.md; issues #19/#20/#21)
    Phase E (native iCloud ubiquity bridge) intentionally DEFERRED 2026-07-06: the S3
    vault (A-D) already syncs iPad/iPhone/web; iCloud would be Apple-only, exclude web,
    and risk divergent copies if run alongside the vault. S3 is the chosen path.
    [x] 16.1 - Phase A (issue #19/W-000019): vaultCrypto.ts (scrypt N=2^17 + HKDF domain separation + AES-256-GCM with vault-id AAD, @noble/hashes) and vaultSync.ts (LWW merge, two-sided tombstones with edit-resurrection, ETag optimistic concurrency with conflict retry, snapshot version guard, injectable LocalNotesPort/VaultTransport). 20 unit tests incl. delete-propagation, updatedAt preservation, 3-device convergence, concurrent-push conflict, tamper/AAD rejection. Survived an adversarial security review round: both HIGH findings (local deletes resurrected; pulled notes re-stamped updatedAt) fixed via port-contract changes before anything shipped
    [x] 16.2 - Phase B (issue #20/W-000020): api/vault routes (register / meta / snapshot GET+PUT) on Vercel Node functions -> S3; writeTokenHash in private auth.json (constant-time bearer check); ETag <-> S3 conditional writes (412 -> client re-merge); 4MB cap; capacitor-origin CORS. Infra provisioned: private bucket mmw-vault-sync-048674617441 + least-privilege IAM user (mmw-sync/ prefix only) + VAULT_S3_* env vars (production; AWS_* names are Vercel-reserved). Client vaultHttpTransport.ts implements the Phase A port; 7 new tests (83 total). Deploy fixes: SPA rewrite now excludes /api (dynamic-segment functions resolve AFTER rewrites) and concurrency rides X-Vault-If-Match/X-Vault-If-None-Match (Vercel's edge consumes the standard conditional headers). Verified LIVE: 20-check curl contract suite (201/409/401/403/404/412/CORS) + full client-stack e2e (real scrypt/AES + syncVault against production S3: pull, edit+delete propagation, wrong-passphrase rejection); test vaults purged from the bucket. Two adversarial security-review passes on the API: (1) HIGH stored-XSS on GET snapshot (@vercel/node send() defaults strings to text/html) fixed with explicit application/json + nosniff + strict base64/no-unknown-keys envelope validation; (2) MEDIUM unauthenticated-create abuse mitigated with a Vercel Firewall rate limit (5/min/IP on POST /api/vault, blocks at edge — verified live); LOW meta.json advisory-only documented, transport 413/404 mapping, constant-time hash guard hardened
    [x] 16.3 - Phase C1 (issue #21/W-000021): data-model backbone. AppApi gains verbatim-updatedAt writeNote + list/record/clearTombstones across all three shims (web IndexedDB v2 tombstone store; iOS .vault-meta.json sidecar for times+tombstones since Capacitor lacks utimes; Electron fs.utimes + .vault-tombstones.json). notesStore.deleteNote records a tombstone; localNotesPort.ts bridges the shim to the engine's port. Real-shim-through-adapter test proves delete propagation into the vault
    [x] 16.4 - Phase C/D (issue #21/W-000021): Cloud Sync UI. vaultSyncController (enable/link/syncNow/disableSync; per-platform transport origin; post-sync reloadLibrary; persists only non-secret settings). SyncModal (passphrase entry never stored/re-collected each sync; enable/link/sync/manage views; iCloud-vs-vault copy; unrecoverable-passphrase warning). Settings "Cloud Sync" section; App hydrates sync settings + on-focus auto-sync nudge (surfaces passphrase prompt when stale >5min, throttled). Verified: Playwright UI smoke (render/nav/validation, no console errors) + LIVE two-device round-trip against production (A enables, B links by code, pulls A's note); test vault purged
    UX decisions (user): auto-sync on focus (reconciled with never-store passphrase by prompting when stale) + passphrase re-entered every sync, never stored
    [x] 16.5 - Phase C/D security+UX review round (Opus, approve-with-nits): friendly "passphrase doesn't match this vault" on decrypt failure (was raw WebCrypto OperationError); busy label paints before the synchronous scrypt freeze (rAF yield); modal a11y — per-view <form> submit (Enter + password managers), Escape-to-close, Tab focus trap, aria-live errors, aria-labelledby; iCloud-vs-vault copy reframed around scope+encryption and gated to iOS; on-return nudge switched to visibilitychange + 30min backoff and can't hijack an open modal; scrim/✕ disabled while busy

[~] 17.0 - Phase 17 - Omarchy/Linux distribution track (#23 -> #25 -> #26 -> #24 -> #29)
    [x] 17.1 - Issue #23 plan synchronized: Linux x86_64 ZIP, Electron/Bun pins, icons, LICENSE, runtime inventory, and validation boundaries (docs/issue-23-IMPLEMENTATION_PLAN.md)
    [~] 17.2 - Issue #23 Linux ZIP implemented, Omarchy smoke-tested, and independently verified; macOS Darwin host check and shipping PR pending
    [ ] 17.3 - Implement #25 launcher/associations/single-instance opening after #23
    [ ] 17.4 - Implement #26 Hyprland integration after #25
    [ ] 17.5 - Implement #24 PKGBUILD/private pacman flow after #23 and integration prerequisites
    [ ] 17.6 - Implement #29 CI/installed-package tests/support guide after the package path exists
